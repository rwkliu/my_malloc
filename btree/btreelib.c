#include "btreelib.h"

/*
 * btreelib.c
    delete
    insert
    traverse
    search
 */

int bt_add(NODE *root)
{
    b
}

int bt_insert(NODE *root, int)
{
    // if no node, create it
    if (node == NULL)
}
